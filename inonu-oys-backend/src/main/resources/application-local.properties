spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:SecretPassword44}
spring.flyway.validate-on-migrate=${FLYWAY_VALIDATE_ON_MIGRATE:false}

# Existing local records must not be changed by bootstrap users during verification.
app.bootstrap.enabled=${BOOTSTRAP_ENABLED:false}
